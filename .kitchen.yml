---
driver:
  name: terraform

provisioner:
  name: terraform

platforms:
- name: local

verifier:
  name: terraform

suites:
  - name: default
    driver:
      root_module_directory: examples/default
    verifier:
      systems:
        - name: default
          backend: local
          attrs:
            - test/integration/default/attrs.yml
